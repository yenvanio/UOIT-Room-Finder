{
    "name": "uoit-room-finder",
    "version": "0.0.0",
    "scripts": {
      "update-submodules": "git submodule update --recursive --remote && git add . && git commit -m 'Updating Submodules' && git push"
    }
  }
  